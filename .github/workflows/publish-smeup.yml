name: Deploy to internal smeup nexus
on:
  workflow_dispatch:
    inputs:
      NEXUS_USER:
        description: 'Nexus user'
        required: true
        type: string
      NEXUS_PASSWORD:
        description: 'Nexus password'
        required: true
        type: string
  push:
    # branches: [master, develop]
    branches: ['feature/add_nexus_deploy_action']
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-java@v1
        with:
          java-version: 1.8
      # save private key to file (private.gpg)
      - run: echo "${{ secrets.GPG_KEY_BASE64 }}" | base64 -d > ~/private.gpg
      # create gradle.properties file
      - run: |
          mkdir -p ~/.gradle/
          echo "GRADLE_USER_HOME=${HOME}/.gradle" >> $GITHUB_ENV
          echo "signing.keyId=${{ secrets.GPG_KEY_ID }}" >> ~/.gradle/gradle.properties
          echo "signing.password=${{ secrets.GPG_PASSPHRASE }}" >> ~/.gradle/gradle.properties
          echo "signing.secretKeyRingFile=${HOME}/private.gpg" >> ~/.gradle/gradle.properties
          echo "smeupUsername=${{ inputs.NEXUS_USER }}"" >> ~/.gradle/gradle.properties
          echo "smeupPassword=${{ inputs.NEXUS_PASSWORD }}" >> ~/.gradle/gradle.properties
          echo "gradle.properties"
          tail ~/.gradle/gradle.properties
      # deploy
      - uses: gradle/gradle-build-action@67421db6bd0bf253fb4bd25b31ebb98943c375e1
        with:
          arguments: publishToSmeup
